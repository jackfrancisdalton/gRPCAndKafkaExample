{
    "name": "@repo/common-config",
    "version": "1.0.0",
    "main": "dist/index.js",
    "types": "dist/index.d.ts",
    "scripts": {
        "clean": "rm -rf dist",
        "build": "pnpm run clean && tsc -p tsconfig.json"
    },
    "dependencies": {
        "dotenv": "^10.0.0"
    }
}